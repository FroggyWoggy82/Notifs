[build]
builder = "NIXPACKS"
buildCommand = "npm install express --no-optional"

[deploy]
startCommand = "node healthcheck-server.js"
healthcheckPath = "/healthcheck"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
