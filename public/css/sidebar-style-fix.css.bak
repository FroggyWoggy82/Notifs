/**
 * Sidebar Style Fix
 * Ensures consistent sidebar styling across all pages to match index.html
 */

/* Fix z-index issues */
.sidebar {
    z-index: 1060 !important; /* Higher than modal backdrop */
    position: fixed !important;
    top: 0 !important;
    left: 0 !important;
    height: 100vh !important;
    width: 280px !important;
    background-color: var(--color-dark-surface, #1A1A1A) !important;
    transform: translateX(-100%) !important;
    transition: transform 0.3s ease !important;
    overflow-y: auto !important;
    box-shadow: 2px 0 10px rgba(0, 0, 0, 0.5) !important;
}

.sidebar.active {
    transform: translateX(0) !important;
}

/* Fix overlay */
.sidebar-overlay {
    position: fixed !important;
    top: 0 !important;
    left: 0 !important;
    width: 100% !important;
    height: 100% !important;
    background-color: rgba(0, 0, 0, 0.7) !important;
    z-index: 1050 !important; /* Lower than sidebar but higher than other content */
    opacity: 0 !important;
    visibility: hidden !important;
    transition: opacity 0.3s ease, visibility 0.3s ease !important;
    pointer-events: none !important;
}

.sidebar-overlay.active {
    opacity: 1 !important;
    visibility: visible !important;
    pointer-events: auto !important;
}

/* Fix for sidebar overlay activation */
body.sidebar-active .sidebar-overlay {
    opacity: 1 !important;
    visibility: visible !important;
    pointer-events: auto !important;
}

/* Fix menu button */
.menu-button {
    z-index: 1070 !important; /* Higher than sidebar */
    position: fixed !important;
    top: 15px !important;
    left: 15px !important;
    background-color: transparent !important;
    border: none !important;
    color: #FFFFFF !important;
    font-size: 1.5rem !important;
    cursor: pointer !important;
    padding: 5px !important;
    margin: 0 !important;
}

/* Fix body when sidebar is active */
body.sidebar-active {
    overflow: hidden !important;
}

/* Sidebar header styling */
.sidebar-header {
    background-color: #121212 !important;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05) !important;
    padding: 15px 20px !important;
    margin: 0 !important;
}

.sidebar-header h2 {
    color: #FFFFFF !important;
    font-size: 1.2rem !important;
    font-weight: 500 !important;
    margin: 0 !important;
    text-align: left !important;
    opacity: 1 !important;
}

/* Sidebar navigation styling */
.sidebar-nav {
    padding: 0 !important;
    margin: 0 !important;
}

.sidebar-nav-item {
    display: flex !important;
    align-items: center !important;
    padding: 12px 20px !important;
    color: rgba(255, 255, 255, 0.9) !important;
    text-decoration: none !important;
    transition: all 0.2s ease !important;
    opacity: 0.9 !important;
    border-left: 3px solid transparent !important;
}

.sidebar-nav-item:hover {
    opacity: 1 !important;
    background-color: rgba(255, 255, 255, 0.1) !important;
    color: #FFFFFF !important;
}

.sidebar-nav-item.active {
    background-color: rgba(255, 255, 255, 0.1) !important;
    color: #FFFFFF !important;
    border-left: 3px solid #FFFFFF !important;
}

.nav-icon {
    margin-right: 12px !important;
    width: 20px !important;
    text-align: center !important;
    font-size: 1rem !important;
}

/* Ensure Font Awesome icons are properly styled */
.sidebar-nav-item .nav-icon i {
    display: inline-block !important;
    font-size: 1.2em !important;
    width: 1.25em !important;
    text-align: center !important;
    vertical-align: middle !important;
}

.sidebar-nav-item.active .nav-icon i {
    color: var(--color-primary, #FFFFFF) !important;
}

/* Adjust content positioning */
.content-wrapper, .container, .main-content, h1, .card, .workout-container, .calendar-container, .food-container, .journal-container, .product-tracking-container {
    margin-left: 0 !important;
    padding-left: 20px !important;
    padding-right: 20px !important;
    transition: margin-left 0.3s ease !important;
}

/* Only apply margin when sidebar is active */
body.sidebar-active .content-wrapper,
body.sidebar-active .main-content,
body.sidebar-active .workout-container,
body.sidebar-active .calendar-container,
body.sidebar-active .food-container,
body.sidebar-active .journal-container,
body.sidebar-active .product-tracking-container {
    margin-left: 280px !important;
}

/* Keep container centered even when sidebar is active */
body.sidebar-active .container {
    margin: 0 auto !important;
    padding-left: 20px !important;
    padding-right: 20px !important;
    max-width: 800px !important;
    transform: translateX(140px) !important; /* Half of sidebar width to keep it centered in remaining space */
}

/* Fix for workouts page specific content */
.workout-tracker, .workout-templates, .exercise-history {
    margin-left: 0 !important;
    transition: margin-left 0.3s ease !important;
}

body.sidebar-active .workout-tracker,
body.sidebar-active .workout-templates,
body.sidebar-active .exercise-history {
    margin-left: 280px !important;
}

/* Specific fixes for index.html */
.container {
    padding-left: 20px !important;
    padding-right: 20px !important;
    max-width: 800px !important;
    margin: 0 auto !important;
}

.container h1 {
    text-align: center !important;
    padding-left: 0 !important;
    width: 100% !important;
}

.task-list-section, .completed-tasks-section, .habit-list-section {
    padding-left: 0 !important;
    padding-right: 0 !important;
    width: 100% !important;
}

/* Fix for task list and habit list cards */
.card {
    width: 100% !important;
    margin-left: auto !important;
    margin-right: auto !important;
}

/* Fix for bottom navigation bar */
.bottom-nav {
    position: fixed !important;
    bottom: 0 !important;
    left: 0 !important;
    width: 100% !important;
    display: flex !important;
    justify-content: center !important;
    background-color: #121212 !important;
    z-index: 1000 !important;
    transition: left 0.3s ease, width 0.3s ease !important;
    padding: 0 !important;
    margin: 0 !important;
}

.bottom-nav .nav-item {
    flex: 1 !important;
    text-align: center !important;
    display: flex !important;
    flex-direction: column !important;
    align-items: center !important;
    justify-content: center !important;
    padding: 10px 0 !important;
    margin: 0 !important;
    max-width: 20% !important; /* Ensure equal width for all items */
}

.bottom-nav .nav-icon {
    display: flex !important;
    justify-content: center !important;
    align-items: center !important;
    margin-bottom: 5px !important;
    width: 100% !important;
}

.bottom-nav .nav-icon i {
    margin: 0 auto !important;
    display: block !important;
}

.bottom-nav .nav-item span {
    display: block !important;
    text-align: center !important;
    width: 100% !important;
}

/* Bottom navigation bar adjustments for sidebar active state */
body.sidebar-active .bottom-nav {
    width: calc(100% - 280px) !important;
    left: 280px !important;
    transition: left 0.3s ease, width 0.3s ease !important;
}

/* Mobile adjustments */
@media (max-width: 768px) {
    .sidebar {
        width: 250px !important;
    }

    body.sidebar-active .content-wrapper,
    body.sidebar-active .main-content,
    body.sidebar-active h1,
    body.sidebar-active .card,
    body.sidebar-active .workout-container,
    body.sidebar-active .calendar-container,
    body.sidebar-active .food-container,
    body.sidebar-active .journal-container,
    body.sidebar-active .product-tracking-container,
    body.sidebar-active .workout-tracker,
    body.sidebar-active .workout-templates,
    body.sidebar-active .exercise-history {
        margin-left: 250px !important;
    }

    body.sidebar-active .container {
        transform: translateX(125px) !important;
    }

    body.sidebar-active .bottom-nav {
        width: calc(100% - 250px) !important;
        left: 250px !important;
    }
}
