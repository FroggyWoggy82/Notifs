/*
 * Habit Card Fix
 * Adjusts the habit card layout to ensure proper alignment
 */

/* Adjust the card content padding */
.card__content {
    padding: 0 !important;
}

/* Specifically target the habit card content */
.card--habits .card__content {
    padding: 0 !important;
}

/* Adjust the card body padding */
.card--habits .card__body {
    padding: 0 !important;
}

/* Ensure the habit list container takes full width */
.card__content #habitList,
.card__body #habitList {
    width: 100% !important;
    padding: 0 !important;
    margin: 0 !important;
    box-sizing: border-box !important;
}

/* Fix habit item alignment */
#habitList .habit-item {
    margin-left: 0 !important;
    margin-right: 0 !important;
    width: 100% !important;
    box-sizing: border-box !important;
}

/* Adjust the card header to match the new layout */
.card__header {
    padding: 16px 16px 8px 16px !important;
    display: flex !important;
    justify-content: space-between !important;
    align-items: center !important;
}

/* Fix the card container */
.card--habits {
    padding: 0 !important;
    width: 100% !important;
    max-width: 100% !important;
    box-sizing: border-box !important;
}

/* Ensure the card title is properly aligned */
.card__title {
    margin: 0 !important;
}

/* Ensure the add habit button is properly aligned */
.card__actions {
    margin-left: auto !important;
}
