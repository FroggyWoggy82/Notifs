/**
 * Goal Checkmark Alignment Fix
 * This CSS aligns the checkmark in the GOAL column with the checkboxes
 */

/* Align the checkmark in the GOAL column with the checkboxes */
.column-headers span:nth-child(6) {
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
    height: 25px !important; /* Match the height of the checkbox */
    vertical-align: middle !important;
}

/* Ensure the checkmark is vertically centered */
.column-headers span:nth-child(6) svg,
.column-headers span:nth-child(6) i {
    vertical-align: middle !important;
    margin: auto !important;
}

/* Ensure the checkmark is properly centered */
.set-row .set-complete-toggle {
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
    margin: 0 auto !important;
    position: relative !important;
}

/* Style the checkmark to ensure it's properly centered */
.set-complete-toggle.completed:after {
    content: '✓';
    position: absolute !important;
    top: 50% !important;
    left: 50% !important;
    transform: translate(-50%, -50%) !important;
    font-size: 16px !important;
    line-height: 1 !important;
}

/* Ensure the goal column is properly aligned */
.goal-target {
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
    height: 25px !important; /* Match the height of the checkbox */
}

/* Ensure the column headers are properly aligned */
.column-headers {
    align-items: center !important;
    display: grid !important;
}

/* Ensure the checkmark column header is properly aligned */
.column-headers span:last-child {
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
    height: 100% !important;
}

/* Ensure the checkmark is properly aligned with the text */
.column-headers span:last-child svg,
.column-headers span:last-child i {
    vertical-align: middle !important;
}

/* Ensure all column headers are properly aligned */
.column-headers span {
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
    height: 100% !important;
}

/* Mobile-specific adjustments */
@media screen and (max-width: 599px) {
    /* Ensure the checkmark is vertically centered in its cell */
    .set-row .set-complete-toggle {
        margin: 0 auto !important;
        align-self: center !important;
    }

    /* Ensure the goal column is vertically centered */
    .goal-target {
        align-self: center !important;
    }

    /* Ensure all cells in the set row are vertically centered */
    #current-exercise-list .set-row > * {
        align-self: center !important;
    }
}
