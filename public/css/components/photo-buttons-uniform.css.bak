/**
 * Photo Buttons Uniform
 * Makes all photo action buttons have consistent styling
 */

/* Center the header buttons container */
.progress-section-header {
    display: flex;
    justify-content: center;
    margin-bottom: 15px;
}

/* Style the header buttons container */
.header-buttons {
    display: flex;
    gap: 15px;
    justify-content: center;
}

/* Make all buttons in the header have the same styling */
.header-buttons .btn {
    background-color: #333 !important;
    color: #f5f5f5 !important;
    border: 1px solid #444 !important;
    transition: all 0.2s ease !important;
    padding: 10px 20px !important;
    font-size: 1rem !important;
    min-width: 120px !important;
}

/* Hover effect for all buttons */
.header-buttons .btn:hover {
    background-color: #444 !important;
    border-color: #555 !important;
}

/* Disabled state for delete button */
.header-buttons .btn:disabled {
    opacity: 0.5 !important;
    cursor: not-allowed !important;
}

/* Ensure the delete button doesn't have red styling */
#delete-photo-btn {
    background-color: #333 !important;
    color: #f5f5f5 !important;
}

/* Ensure the compare button doesn't have primary styling */
#compare-photos-btn {
    background-color: #333 !important;
    color: #f5f5f5 !important;
}
