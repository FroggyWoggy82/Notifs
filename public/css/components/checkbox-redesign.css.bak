/**
 * Checkbox Redesign
 * Redesigns task checkboxes to be larger and more visually appealing
 */

/* Task checkboxes - with high specificity to override other styles */
html body .task-item input[type="checkbox"] {
    /* Larger size */
    width: 24px !important;
    height: 24px !important;

    /* Appearance */
    appearance: none !important;
    -webkit-appearance: none !important;
    background-color: rgba(18, 18, 18, 0.9) !important;
    border: 2px solid rgba(255, 255, 255, 0.4) !important;
    border-radius: 50% !important;

    /* Positioning */
    margin: 0 !important;
    padding: 0 !important;
    position: relative !important;
    align-self: center !important;

    /* Effects */
    cursor: pointer !important;
    box-shadow: 0 0 8px rgba(255, 255, 255, 0.1) !important;
    transition: all 0.2s ease !important;

    /* Reset any transformations */
    transform: none !important;
    -webkit-transform: none !important;
    -moz-transform: none !important;
    -ms-transform: none !important;
}

/* Hover state */
html body .task-item input[type="checkbox"]:hover {
    border-color: rgba(255, 255, 255, 0.7) !important;
    box-shadow: 0 0 12px rgba(255, 255, 255, 0.2) !important;
}

/* Checked state */
html body .task-item input[type="checkbox"]:checked {
    background-color: rgba(255, 255, 255, 0.1) !important;
    border-color: rgba(255, 255, 255, 0.8) !important;
    box-shadow: 0 0 15px rgba(255, 255, 255, 0.3) !important;
}

/* Create checkmark using ::after pseudo-element */
html body .task-item input[type="checkbox"]:checked::after {
    content: '';
    position: absolute !important;
    top: 50% !important;
    left: 50% !important;
    transform: translate(-50%, -50%) !important;
    width: 12px !important;
    height: 12px !important;
    background-color: rgba(255, 255, 255, 0.9) !important;
    border-radius: 50% !important;
    box-shadow: 0 0 8px rgba(255, 255, 255, 0.4) !important;
}

/* Checkbox container styling */
html body .task-checkbox-container {
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
    margin-right: 12px !important;
    min-width: 24px !important;
    min-height: 24px !important;
}

/* Adjust task content to align with the new checkbox size */
html body .task-content {
    display: flex !important;
    flex-direction: column !important;
    justify-content: center !important;
    flex: 1 !important;
    min-height: 24px !important;
}

/* Habit checkboxes - with high specificity to override other styles */
html body .habit-item .habit-checkbox {
    /* Larger size */
    width: 32px !important;
    height: 32px !important;

    /* Appearance */
    appearance: none !important;
    -webkit-appearance: none !important;
    background-color: rgba(18, 18, 18, 0.9) !important;
    border: 2px solid rgba(255, 255, 255, 0.4) !important;
    border-radius: 50% !important;

    /* Positioning */
    margin: 0 !important;
    padding: 0 !important;
    position: relative !important;

    /* Effects */
    cursor: pointer !important;
    box-shadow: 0 0 8px rgba(255, 255, 255, 0.1) !important;
    transition: all 0.2s ease !important;
}

/* Hover state for habit checkboxes */
html body .habit-item .habit-checkbox:hover {
    border-color: rgba(255, 255, 255, 0.7) !important;
    box-shadow: 0 0 12px rgba(255, 255, 255, 0.2) !important;
}

/* Checked state for habit checkboxes */
html body .habit-item .habit-checkbox:checked {
    background-color: rgba(255, 255, 255, 0.1) !important;
    border-color: rgba(255, 255, 255, 0.8) !important;
    box-shadow: 0 0 15px rgba(255, 255, 255, 0.3) !important;
}

/* Create checkmark using ::after pseudo-element for habit checkboxes */
html body .habit-item .habit-checkbox:checked::after {
    content: '';
    position: absolute !important;
    top: 50% !important;
    left: 50% !important;
    transform: translate(-50%, -50%) !important;
    width: 16px !important;
    height: 16px !important;
    background-color: rgba(255, 255, 255, 0.9) !important;
    border-radius: 50% !important;
    box-shadow: 0 0 8px rgba(255, 255, 255, 0.4) !important;
}

/* Habit control container styling */
html body .habit-control-container {
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
    margin-right: 12px !important;
    min-width: 32px !important;
    min-height: 32px !important;
}

/* Adjust task item layout to accommodate larger checkboxes */
html body .task-item {
    padding: 12px 16px !important;
    min-height: 48px !important;
    display: flex !important;
    align-items: center !important;
}

/* Ensure proper spacing in task items */
html body .task-item > * {
    margin-right: 12px !important;
}

html body .task-item > *:last-child {
    margin-right: 0 !important;
}
