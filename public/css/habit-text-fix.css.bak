/* 
 * Habit Text Fix
 * Fixes issues with truncated text in habit items
 */

/* Ensure habit item has enough height for content */
.habit-item {
    min-height: 60px !important;
    height: auto !important;
}

/* Fix habit title display */
.habit-title {
    display: block !important;
    white-space: normal !important;
    overflow: visible !important;
    text-overflow: clip !important;
    word-break: break-word !important;
    line-height: 1.2 !important;
    margin-bottom: 4px !important;
}

/* Fix frequency text display */
.habit-frequency {
    display: block !important;
    white-space: normal !important;
    overflow: visible !important;
    text-overflow: clip !important;
    word-break: normal !important;
    line-height: 1.2 !important;
}

/* Ensure content area has enough space */
.habit-content {
    padding-right: 16px !important;
    min-width: 0 !important;
    flex: 1 !important;
    max-width: 60% !important;
}

/* Ensure indicators don't shrink too much */
.habit-indicators-row {
    flex-shrink: 0 !important;
    white-space: nowrap !important;
}

/* Ensure progress container doesn't shrink */
.habit-progress-container {
    flex-shrink: 0 !important;
}

/* Ensure level indicator doesn't shrink */
.habit-level {
    flex-shrink: 0 !important;
}

/* Media query for smaller screens */
@media screen and (max-width: 768px) {
    .habit-content {
        max-width: 100% !important;
        margin-bottom: 8px !important;
    }
    
    .habit-indicators-row {
        width: 100% !important;
        justify-content: flex-end !important;
    }
}
