/**
 * Next Date Prefix
 * DISABLED - This component was causing duplicate "Next:" badges
 * The main task creation logic now handles "Next:" prefixes correctly
 */

// Component disabled to prevent duplicate "Next:" badges
// The main task rendering logic in script.js now properly handles next occurrence dates
console.log('next-date-prefix.js: Component disabled to prevent duplicate badges');
